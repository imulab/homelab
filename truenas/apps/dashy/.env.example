# Dashy takes no secrets. This file exists for consistency with the per-stack
# convention (decision #5): a comment-only .env.example signals "no secrets"
# explicitly, distinct from "we forgot". If you later add a secret, document it
# here and set the value in Dockhand's Stack editor env-var panel.
